# Task 104: Track Entität Erweiterung (DNA Properties)

## Ziel
Hinzufügen der Mood- und TimeContext-Eigenschaften zur `Track`-Entität.

## Details
- `Mood` vom Typ `TrackMood` in `Track.cs` hinzufügen.
- `TimeContext` vom Typ `TrackTimeContext` in `Track.cs` hinzufügen.
- `INotifyPropertyChanged` sicherstellen.

## Fortschritt
- [x] Properties in `Track.cs` implementiert.
- [x] UI-Notification Support sichergestellt.
